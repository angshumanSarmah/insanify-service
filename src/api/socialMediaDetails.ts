import { SocialMediaDetailsModel } from "../model/socialMediaModel";

const so: SocialMediaDetailsModel = {
    user_id: 'string',
    twitter: {
        userName: 'string',
        followers: 'string',
        reach: 'string'
    }
} 